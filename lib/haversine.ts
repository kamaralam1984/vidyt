/**
 * Haversine formula to calculate distance between two lat/long coordinates.
 * @returns Distance in kilometers
 */
export function haversineDistance(
  lat1: number,
  lon1: number,
  lat2: number,
  lon2: number
): number {
  const R = 6371; // Earth radius in km
  const dLat = ((lat2 - lat1) * Math.PI) / 180;
  const dLon = ((lon2 - lon1) * Math.PI) / 180;
  const a =
    Math.sin(dLat / 2) * Math.sin(dLat / 2) +
    Math.cos((lat1 * Math.PI) / 180) *
      Math.cos((lat2 * Math.PI) / 180) *
      Math.sin(dLon / 2) *
      Math.sin(dLon / 2);
  const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
  return Math.round(R * c);
}

// Admin's fixed lat/long (can be configured via env)
export const ADMIN_LAT = parseFloat(process.env.ADMIN_LAT || '28.6139'); // Default: New Delhi
export const ADMIN_LON = parseFloat(process.env.ADMIN_LON || '77.2090');
